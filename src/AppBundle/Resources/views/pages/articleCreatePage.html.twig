{#
    params : 
        - articleForm : ArticleCreateForm object
#}
{% extends '::base.html.twig' %}
{% block title %}
    {{ parent() }} - {{ 'app.global.create_article' | trans }}
{% endblock %}
{% block body %}
    <div class="row">
        <div class="col-md-12">
            <ol class="breadcrumb">
                <li>
                    <a href="{{ path('homepage') }}">{{ 'app.global.home' | trans }}</a>
                </li>
                <li class="active">
                    {{ 'app.global.create_article' | trans }}
                </li>
            </ol>
        </div>
    </div>
    <h1>{{ 'app.global.create_article' | trans }}</h1>
    <div class="row">
        <div class="col-md-12">
            {% include 'AppBundle:forms:articleForm.html.twig' with { 'form' : articleForm } %}
        </div>
    </div>
{% endblock %}