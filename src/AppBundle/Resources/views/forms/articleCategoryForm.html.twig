{#
    params : 
        - form : ArticleCategoryForm object
#}
{{ form_start(form, { 
    'attr': {
        'role' : 'form',
        'id' : 'article-category-form'
    },
    'action' : path('admin_category_create')
}) }}
    {{ form_row(form.articleCategory) }}
    <button class="btn btn-primary" type="submit">{{ 'app.submit' | trans | capitalize }}</button>
{{ form_end(form) }}