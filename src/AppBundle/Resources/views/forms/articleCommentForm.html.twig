{#
    params : 
        - form : ArticleCommentForm object
#}
{{ form_start(form) }}
    {{ form_row(form.comment) }}
    <button class="btn btn-primary" type="submit">Submit</button>
{{ form_end(form) }}