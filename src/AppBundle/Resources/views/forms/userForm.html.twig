{{ form_start(form) }}
    {{ form_row(form.email) }}
    {{ form_row(form.name) }}
    {{ form_row(form.plainPassword) }}
    <button class="btn btn-primary" type="submit" formnovalidate>Ok</button>
{{ form_end(form) }}